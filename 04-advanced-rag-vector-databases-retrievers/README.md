# Course 4 — Advanced RAG with Vector Databases and Retrievers

**Coursera course:** [https://www.coursera.org/learn/[course-slug]](https://www.coursera.org/learn/[course-slug])
**Part of:** [IBM RAG and Agentic AI Specialization](../README.md)
**Status:** 🟡 In progress (Lab 1 done)

## Learning goals

- Improve retrieval quality with re-ranking, hybrid search, and query transformation
- Use advanced retriever types (multi-query, parent-document, self-query, MMR)
- Evaluate a RAG system (relevance, groundedness, answer quality)
- Tune chunking and retrieval parameters against measured results

## Labs

### Lab 1 — Build a Smarter Search with LangChain Context Retrieval

- **Task:** Move beyond a single vector-store similarity search and work through four LangChain retriever types — Vector Store-Backed, Multi-Query, Self-Querying, and Parent Document — understanding what problem each one solves and when to reach for it. Includes 2 exercises.
- **Approach:**
  - **LLM/embeddings setup:** `mistralai/mistral-small-3-1-24b-instruct-2503` via `WatsonxLLM` for query generation/reasoning, `ibm/granite-embedding-278m-multilingual` via `WatsonxEmbeddings` for all vector stores (Chroma).
  - **Vector Store-Backed Retriever:** loaded a company-policies text file, split it (`RecursiveCharacterTextSplitter`, 200/20), embedded into Chroma, then compared three `as_retriever()` search modes on the same query ("email policy"): default top-4 similarity, `search_kwargs={"k": 1}` to cap results, `search_type="mmr"` (Maximal Marginal Relevance — trades pure similarity for result diversity, avoiding redundant near-duplicate chunks), and `search_type="similarity_score_threshold"` to filter out low-relevance matches entirely instead of always returning a fixed k.
  - **Multi-Query Retriever:** loaded a LangChain research PDF (`PyPDFLoader`), re-embedded it, then wrapped the vector store retriever in `MultiQueryRetriever.from_llm()` — the LLM rewrites one user query into 3 differently-phrased variants (logged via Python's `logging` module), retrieves for each, and returns the union of results. This compensates for a single query's wording not matching how relevant chunks were phrased.
  - **Self-Querying Retriever:** built a small movie dataset with metadata (`year`, `rating`, `genre`, `director`) plus an `AttributeInfo` schema describing those fields, then used `SelfQueryRetriever.from_llm()` so the LLM translates a natural-language ask ("a movie rated higher than 8.5", "directed by Christopher Nolan") into a **structured filter** applied against the metadata — combining semantic search with exact attribute filtering in one call, comparable to the metadata `where` filtering used with ChromaDB directly in Course 3, but with the filter itself generated by the LLM instead of written by hand.
  - **Parent Document Retriever:** split the same policy document with two splitter granularities — small "child" chunks (200 chars, for accurate embeddings) and larger "parent" chunks (1000 chars, for retained context) — indexing only the child chunks in Chroma while keeping parent documents in an `InMemoryStore`. On retrieval, the small chunk is matched first, but the retriever returns its *parent* chunk, resolving the classic trade-off between embedding precision (small chunks) and context sufficiency (large chunks).
  - **Exercises:** (1) reran the Vector Store-Backed Retriever with `k=2` against "smoking policy", explicitly clearing the Chroma collection first to avoid mixing leftover embeddings from the PDF/movie sections into the results; (2) reran the Self-Querying Retriever with a director-name filter ("movie directed by Christopher Nolan"), again clearing the collection first — both exercises required realizing that Chroma's default collection persists across cells and must be reset before switching datasets, which the lab's own base code doesn't do.
- **Code:** [`labs/Build a Smarter Search with LangChain Context Retrieval.ipynb`](<./labs/Build%20a%20Smarter%20Search%20with%20LangChain%20Context%20Retrieval.ipynb>)
- **Key learning:** "Retriever" isn't one algorithm, it's an interface with several strategies behind it, each fixing a different weakness of plain similarity search: MMR fixes redundant results, Multi-Query fixes query-wording sensitivity by asking the same question multiple ways, Self-Querying fixes the "I need semantic search AND an exact filter" case by having the LLM write the filter instead of the developer, and Parent Document fixes the precision-vs-context trade-off in chunk sizing. Also, a shared vector store collection is stateful — reusing `vectordb` across sections without clearing it silently mixes datasets, a failure mode that only shows up as wrong (not missing) results, which made explicitly deleting existing embeddings before re-populating necessary in both exercises.

## Key takeaways

- MMR search (`search_type="mmr"`) trades strict top-k similarity for diversity — useful when top results risk being near-duplicates of each other.
- `similarity_score_threshold` search returns only matches above a relevance cutoff instead of always forcing a fixed number of results, which avoids padding results with weak matches.
- `MultiQueryRetriever` uses an LLM to rephrase one query into several, then unions the retrieved results — a cheap way to make retrieval robust to how a query happens to be worded.
- `SelfQueryRetriever` has the LLM translate a natural-language request into a structured metadata filter (given an `AttributeInfo` schema), combining semantic search with exact filtering without the developer hand-writing the filter logic.
- `ParentDocumentRetriever` indexes small chunks for embedding accuracy but returns their larger parent chunk on retrieval — resolving the chunk-size trade-off between semantic precision and retained context.
- A vector store collection reused across cells/datasets is stateful and must be explicitly cleared (`vectordb.delete(ids)`) before repopulating with different documents, or results silently mix old and new data.

## Tools & libraries

- Python, Jupyter Notebook
- LangChain — `langchain`, `langchain-community`, `langchain-core`, `langchain-text-splitters`; retrievers: `MultiQueryRetriever`, `SelfQueryRetriever`, `ParentDocumentRetriever`
- IBM watsonx.ai (`ibm-watsonx-ai`, `langchain-ibm`) — `WatsonxLLM` (`mistralai/mistral-small-3-1-24b-instruct-2503`), `WatsonxEmbeddings` (`ibm/granite-embedding-278m-multilingual`)
- ChromaDB (`chromadb`) — vector store backing every retriever type in this lab
- `pypdf` (PDF loading), `lark` (query-constructor parsing required by `SelfQueryRetriever`)
